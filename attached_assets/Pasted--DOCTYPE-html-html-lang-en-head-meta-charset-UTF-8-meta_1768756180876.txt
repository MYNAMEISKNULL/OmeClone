<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Site Under Maintenance</title>
    <style>
        /* Base Styles */
        body, html {
            margin: 0;
            padding: 0;
            width: 100%;
            height: 100%;
            background-color: #1a1a1a;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden; /* Prevents scrollbars during animation */
            color: white;
        }

        /* Text Container */
        .content {
            text-align: center;
            z-index: 10;
        }

        h1 {
            font-size: 5rem;
            margin-bottom: 0;
            font-weight: bold;
        }

        h2 {
            font-size: 1.5rem;
            margin: 10px 0;
            font-weight: 300;
        }

        .main-bar {
            font-size: 3rem;
            font-weight: 800;
            text-transform: uppercase;
            border-bottom: 4px solid white;
            display: inline-block;
            padding-bottom: 5px;
            letter-spacing: 2px;
        }

        .coming-soon {
            margin-top: 20px;
            font-size: 1.2rem;
            letter-spacing: 5px;
            color: #ccc;
        }

        /* Construction Tape Styles */
        .tape {
            position: absolute;
            background-color: #ffcc00;
            color: #000;
            height: 60px;
            line-height: 60px;
            font-weight: 900;
            text-transform: uppercase;
            white-space: nowrap;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0,0,0,0.5);
            font-size: 1.2rem;
            width: 0; /* Start at 0 for animation */
            transition: width 1.5s ease-out;
        }

        /* The "Under Construction" Repeating Text */
        .tape::before {
            content: "⚠ UNDER CONSTRUCTION ⚠ UNDER CONSTRUCTION ⚠ UNDER CONSTRUCTION ⚠ UNDER CONSTRUCTION ⚠ UNDER CONSTRUCTION ⚠ UNDER CONSTRUCTION";
            padding-left: 20px;
        }

        /* Specific Tape Positions & Animations */
        
        /* Top Left Tape */
        .tape-1 {
            top: 15%;
            left: -10%;
            transform: rotate(-15deg);
            animation: extend-tape 1.2s forwards ease-in-out;
        }

        /* Top Right Tape */
        .tape-2 {
            top: 5%;
            right: -10%;
            transform: rotate(10deg);
            animation: extend-tape 1.5s forwards 0.3s ease-in-out;
            transform-origin: right;
        }

        /* Bottom Left Tape */
        .tape-3 {
            bottom: 10%;
            left: -5%;
            transform: rotate(15deg);
            animation: extend-tape 1s forwards 0.1s ease-in-out;
        }

        /* Bottom Right Tape */
        .tape-4 {
            bottom: 20%;
            right: -10%;
            transform: rotate(-8deg);
            animation: extend-tape 1.8s forwards 0.5s ease-in-out;
            transform-origin: right;
        }

        /* Animation Keyframes */
        @keyframes extend-tape {
            from { width: 0; }
            to { width: 120%; }
        }

        /* Subtle pulsing effect for the text */
        @keyframes pulse {
            0% { opacity: 0.8; }
            50% { opacity: 1; }
            100% { opacity: 0.8; }
        }

        .coming-soon {
            animation: pulse 2s infinite;
        }
    </style>
</head>
<body>

    <div class="tape tape-1"></div>
    <div class="tape tape-2"></div>
    <div class="tape tape-3"></div>
    <div class="tape tape-4"></div>

    <div class="content">
        <h1>Oops!</h1>
        <h2>Website is</h2>
        <div class="main-bar">Under Construction</div>
        <div class="coming-soon">COMING SOON...</div>
    </div>

</body>
</html>