<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bouncing Dots Loader</title>
    <style>
        /* --- LAYOUT SETUP (Ignore this when copying to your site) --- */
        body {
            background-color: #1a1a1d; /* Dark Chat Background */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            font-family: 'Segoe UI', sans-serif;
            color: white;
        }

        /* --- LOADER CONTAINER --- */
        .loader-wrapper {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 15px; /* Space between dots and text */
        }

        /* --- THE DOTS CONTAINER --- */
        .typing-dots {
            display: flex;
            gap: 8px; /* Space between dots */
        }

        /* --- INDIVIDUAL DOT STYLING --- */
        .dot {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            /* Animation: name duration timing-function delay iteration-count direction fill-mode */
            animation: bounce 1.4s infinite ease-in-out both;
        }

        /* Color & Delay for Dot 1 (Cyan) */
        .dot:nth-child(1) {
            background-color: #00f2ff;
            animation-delay: -0.32s;
        }

        /* Color & Delay for Dot 2 (Purple) */
        .dot:nth-child(2) {
            background-color: #bc13fe;
            animation-delay: -0.16s;
        }

        /* Color & Delay for Dot 3 (Pink) */
        .dot:nth-child(3) {
            background-color: #ff0055;
            animation-delay: 0s;
        }

        /* --- STATUS TEXT STYLING --- */
        .status-text {
            font-size: 0.9rem;
            color: rgba(255, 255, 255, 0.5);
            letter-spacing: 0.5px;
            font-weight: 400;
        }

        /* --- ANIMATION KEYFRAMES --- */
        @keyframes bounce {
            0%, 80%, 100% { 
                transform: scale(0); 
                opacity: 0.5;
            }
            40% { 
                transform: scale(1); 
                opacity: 1;
            }
        }
    </style>
</head>
<body>

    <div class="loader-wrapper">
        <div class="typing-dots">
            <div class="dot"></div>
            <div class="dot"></div>
            <div class="dot"></div>
        </div>
        <div class="status-text">Stranger is typing...</div>
    </div>

</body>
</html>